[project]
name = "jobsignal"
version = "0.0.1"
description = "Job posting verification (JobSignal)"
requires-python = ">=3.10"
readme = "README.md"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
