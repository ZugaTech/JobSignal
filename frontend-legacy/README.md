# frontend/

Sprint 3 **minimal static UI**: open `index.html` in a browser (or serve the folder). Files:

- `index.html` — form + result panels + `data-ui-phase` / `data-cache` hooks  
- `app.js` — phase machine, mock verify response (replace with API `fetch`)  
- `styles.css` — compact judge-friendly styling  

Smoke tests in `tests/test_frontend_smoke.py` assert required phases and copy exist.
